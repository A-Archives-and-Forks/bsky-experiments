services:
  stats:
    build:
      context: ../../
      dockerfile: build/stats/Dockerfile
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
    image: stats
    container_name: stats
    env_file:
      - ../../.stats.env
    ports:
      - "28081:8081"
